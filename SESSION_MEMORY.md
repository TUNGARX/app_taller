# Session Memory and Developer Log

This file tracks the current state of development and next-session priorities. **Instruction for Claude:** At the end of every work session, you must update this file with the accomplished goals, technical findings, and immediate next steps.

## 🗓️ Last Updated: 2026-07-31 (Citas: hide old Completada entries)

## 🚦 Overall Project Status
- **Architecture Decision:** Unified **Next.js (TypeScript + Tailwind + App Router)** application. No separate backend — API logic lives in `src/app/api/*` Route Handlers.
- **Design Direction:** Industrial/workshop aesthetic — charcoal (`steel`) chrome with safety-orange/amber accents, `Bebas Neue` display font for headers, `IBM Plex Sans` for body, `IBM Plex Mono` for plate numbers/IDs/figures. Defined via CSS variables in `src/app/globals.css`. **Do not re-add Tailwind's `antialiased` class to `<html>`** — it forces grayscale font smoothing which looks fine on macOS but makes text look thin/blurry on Windows (overrides ClearType). Removed in this session; if blur complaints resurface, check this first before assuming it's a display-scaling issue.
- **`src/app/globals.css` neutralizes `input:-webkit-autofill`** so Chrome can't inject its own background/text color into autofilled fields inconsistently between resting/focused/active state — this was the root cause of a real bug: input text/placeholders going invisible specifically while focused. Don't remove this rule. (Superseded detail: `color-scheme` used to be hardcoded to `light` when the app was light-only; it's now `inherit` on form controls and flips with the theme — see the Dark Mode section below.)
- **Project Scaffolding / Mock DB / Kanban / Buscar / Citas:** ✅ Done (see prior entries).
- **Role-switch login simulator:** ✅ Done — see below.
- **Cotización → Orden → Factura lifecycle (13% IVA, labor/parts split, tri-state approval, PDF export):** ✅ Done — see below.
- **Photo-upload stub for mechanics:** ✅ Done — see below.
- **Client questionnaire reconciliation (8-stage Kanban, plate-based ownership transfer, order reopening, mechanic price-hiding, vehicle history + notes thread, intake fields):** ✅ Done — see below. This was a substantial rework of the data model; read the "Client Questionnaire" section before touching Kanban stages, Cotizacion status, or OrdenTrabajo fields.
- **Per-card activity log ("who did what, when") + named worker identity:** ✅ Done — see below. Client asked for this so multiple people sharing a role stay distinguishable on a shared Kanban card.
- **Real authentication (Auth.js/NextAuth, self-hosted, username+password, server-enforced roles):** ✅ Done — see below. Replaces the old client-only role simulator entirely. The "who did what" activity log is now attributed from a verified session, not a free-text name.
- **Full mobile responsive pass (every screen usable on phone browsers, not just PC):** ✅ Done — see below. Client explicitly requires the app to work equally well on mobile and desktop.
- **Git/GitHub set up** (`https://github.com/TUNGARX/app_taller`, `main` branch, SSH auth) — ✅ Done. See Blockers for the credential-manager gotcha hit while setting this up.
- **Real logo integrated + Cita status progression + final loose-end triage:** ✅ Done — see below.
- **Public portal (`/buscar`) now shows evidence photos + lets clients download their own Factura PDF:** ✅ Done — see below.
- **Light/dark mode toggle, working on both PC and mobile:** ✅ Done — see below. Client explicitly asked for this as an aesthetic option, available everywhere (landing, portal, staff dashboard).
- **Delete-account button on `/staff`, and 72h auto-archiving for "Entregado" Kanban cards:** ✅ Done — see below. The open question from last session (does Entregado auto-hide?) is now resolved: yes, after 72h, with a reveal toggle and a direct "back to Ingresado" action for warranty returns.
- **Plate search bar on the Panel (Kanban board):** ✅ Done — see below. Lets staff find a card by plate across all columns, and auto-reveals a matching card even if it's hidden by the 72h Entregado archive filter.
- **"Próximas Citas" no longer grows unbounded — old Completada entries drop off after 1 month:** ✅ Done — see below.
- **Database Connection (real Google Sheets API):** ❌ Not started — intentionally deferred until UI is fully prototyped offline. **This is the next session's stated focus.** Architecture decided: a **Google Cloud service account** (not per-user OAuth) — the service account gets shared access to the Sheets/Drive/Calendar resources directly, no consent screen or user tokens needed. Building/testing against the developer's own Google account first; moving to the client's real account later is just sharing the same resources with the service account + swapping a couple of ID env vars, not a rebuild.
- **Third-Party API Integrations (Google Drive / WhatsApp Business Cloud API / Google Calendar for citas):** ❌ Not started.

## 🎯 Completed Work Today
- [x] **Fixed a Windows text-rendering complaint**: removed the `antialiased` class from `src/app/layout.tsx` — see Design Direction note above.
- [x] **Added a "Nueva Orden" button on the Panel** (`src/components/kanban/NuevaOrdenForm.tsx`): lets staff open a work order directly in "Por Revisar" for an existing vehicle or a brand-new walk-in client, without going through the appointments flow. Backed by `crearOrden()` in mock-db (shares the same `resolverClienteVehiculo` helper as `crearCita()`) and `POST /api/ordenes`.
- [x] **Built the role-switch login simulator** (the user's request):
  - `src/lib/role-context.tsx`: a `RoleProvider` React Context storing the active `Rol` (`Owner`/`Secretary`/`Mechanic`) in `localStorage`, wrapping the whole app from `src/app/layout.tsx`.
  - Landing page (`/`) is now the role picker (`src/components/RoleSelector.tsx`) instead of static nav links; picking a role navigates to `/dashboard`.
  - `StaffNav` shows the active role badge + a "Cambiar" (switch role) control; the `(dashboard)` route-group layout (`src/app/(dashboard)/layout.tsx`) redirects to `/` if no role is set (client-side gate, not real auth — there's no server-side enforcement, it's a UI simulation only).
  - `src/components/RoleGuard.tsx`: reusable guard that shows a "not available for your role" message; wraps `/citas` (`Owner`/`Secretary` only).
  - Role-based feature gating implemented: `+ Nueva Orden` and `/citas` → Owner/Secretary only; creating/approving a Cotización → Owner/Secretary only; uploading evidence photos → Owner/Mechanic only. All three roles can move Kanban cards and view order details.
- [x] **Built the Cotización creation/approval UI** (`src/components/kanban/OrdenDetalleModal.tsx`, opened via a new "Ver detalle / cotización / fotos" link on each `OrdenCard`):
  - If no Cotización exists yet and the role allows it, shows a line-item form (descripción/cantidad/precio) with a live-computed total; submits to the new `POST /api/cotizaciones`.
  - If one exists, shows the items, total, and an approve/un-approve toggle (`PATCH /api/cotizaciones/[id]`) for Owner/Secretary.
  - mock-db: `crearCotizacion()` (rejects a second quote on the same order, computes total server-side rather than trusting the client) and `setCotizacionAprobada()`.
- [x] **Built the photo-upload stub** in the same detail modal: a file input (Owner/Mechanic only) reads the chosen image as a `data:` URL via `FileReader` (deliberately not a `blob:` URL — those don't persist reliably across navigations; a data URL is a plain string that behaves like real stored data for the prototype) and posts it to the new `POST /api/ordenes/[id]/fotos`, which appends it to `OrdenTrabajo.fotos`. Existing photos render as thumbnails.
- [x] Refactored `crearCita`/`crearOrden` to share one `resolverClienteVehiculo()` helper in mock-db instead of duplicating the existing-vs-new-registration logic.
- [x] `npx tsc --noEmit` and `npm run lint` clean throughout; verified every new flow both via direct API calls (node `fetch`) and by driving the actual UI in the Browser tool across all three roles (confirmed nav links, buttons, and the detail modal correctly show/hide per role, and that `/citas` redirects Mechanics away).

## 🎯 Completed Work (this round — Cotización fixes + PDF export)
User feedback: the quote-line form only let you add lines, never remove one added by mistake; and once a quote is approved, the shop needs a downloadable PDF matching their real paper quote format (user attached `Henry Aguero Frenos Cot preliminar.pdf` from their shop "Automotivo" as the reference).
- [x] **Fixed the missing remove-line bug** in `OrdenDetalleModal.tsx`: each line in the quote-creation form now has a "✕" button (`quitarLinea()`); disabled/hidden when only one line remains so the form can never go to zero lines.
- [x] **Extended the Cotización data model** (confirmed with the user via AskUserQuestion before building, since this affects real money amounts):
  - `CotizacionItem` now has a `tipo: "Repuesto" | "Mano de Obra"` field (labor vs. parts), picked via a `<select>` per line.
  - `precioUnitario` is now treated as the **pre-tax (neto)** unit price; a new shared helper `src/lib/cotizacion-calc.ts` (`calcularTotalesCotizacion`) computes IVA at Costa Rica's standard **13%** rate, plus labor/parts subtotals — this is the single place that math happens, used by both the on-screen quote UI and the PDF, so they can never disagree.
  - `Cotizacion.total` is now the **grand total with IVA included** (previously was a flat sum with no tax). Updated `crearCotizacion()` in mock-db and the 4 seeded mock quotes accordingly (recalculated their `total` values).
- [x] **Built the Cotización PDF export**, matching the shop's reference layout:
  - `src/lib/taller-config.ts`: business identity (name "Automotivo", slogan, phone/WhatsApp/email, hours, address) taken directly from the reference PDF — **the user should confirm this is correct**, since I inferred it from their attached file rather than being told explicitly. Edit this one file if anything's wrong or changes.
  - `src/components/kanban/CotizacionPdfDocument.tsx`: the `@react-pdf/renderer` document layout (header, datos del cliente/vehículo, Concepto/Observaciones, the Detalle/Cant./PUni./Desc./Subtotal/IVA/Total table, Total mano de obra / Total repuestos / Total Neto / Total IVA / Total, firma lines) — mirrors the reference quote closely.
  - `src/components/kanban/DescargarCotizacionPdfButton.tsx`: client component, dynamically imports `@react-pdf/renderer` + the document on click (keeps the (fairly large) PDF library out of the initial page bundle), generates a blob, and triggers a download named `Cotizacion-{placa}-{fecha}.pdf`. Only rendered once `cotizacion.aprobadaCliente` is true, per the user's request.
  - Added the `@react-pdf/renderer` npm dependency.
- [x] Verified the whole flow live in the browser (create a 2-line quote with one Repuesto + one Mano de Obra line, confirmed the IVA math, approved it, clicked "Descargar PDF", confirmed the `@react-pdf/renderer` chunks loaded and the button completed with no console errors) and generated + sent the user an actual sample PDF built from real seed data so they could see the true rendered output.
- [x] `npx tsc --noEmit` / `npm run lint` clean.
- [x] **Fixed a broken currency glyph in the PDF**: the ₡ (colón) symbol isn't in the PDF's base Helvetica font, so it rendered as a garbled "¡". Added `formatColonesPdf()` in `src/lib/format.ts` (uses `Intl.NumberFormat` with `currencyDisplay: "code"` → "CRC 45 000" instead of "₡45,000") and switched `CotizacionPdfDocument.tsx` to use it — this also happens to match the user's original reference PDF exactly, which used "CRC" text, not the symbol. **Rule of thumb going forward: any new text rendered inside a PDF must stick to characters in Helvetica's WinAnsi encoding (basically Latin-1) — no ₡, no emoji, no exotic Unicode symbols. Accented Spanish characters (á é í ó ú ñ) and · are fine, they're in Latin-1.** Verified by regenerating the sample PDF and reading it back.
- [x] **First pass at "placeholders invisible in Chrome" was wrong** — added `color-scheme: light` + autofill neutralization to `globals.css` (still good practice, kept it), but the user reported the issue persisted. The real tell: **enabling Windows Night Light made the placeholders visible again.** Night Light shifts the whole screen's color temperature/gamma — that only reveals a problem if the actual issue is a *genuinely low-contrast color*, not a focus-state logic bug. Confirmed the math: `placeholder:text-ink/30` (our near-black ink at 30% opacity over white) computes to roughly `rgb(187,186,185)` — contrast ratio ~1.8:1 against white, well under the ~3:1 usability floor, so it was borderline invisible on plenty of displays even without any focus/dark-mode involvement.
  - **Fixed**: bumped every `placeholder:text-ink/30` → `placeholder:text-ink/50` (roughly doubles the effective contrast) across the 4 places it appeared: `src/components/citas/CitaScheduler.tsx`, `src/components/kanban/NuevaOrdenForm.tsx`, `src/components/kanban/OrdenDetalleModal.tsx`, `src/app/(portal)/buscar/page.tsx`. Verified via the Browser tool by reading the computed `::placeholder` style directly — opacity went from 0.3 to 0.5 as expected.
  - **Lesson for next time**: don't trust "it happens on focus" as literally true when it's user-reported through the lens of "when did I notice it" — a low-contrast color is easy to miss until you're staring right at the field you just clicked into. If a color/visibility bug report doesn't fully add up, ask what changes make it better/worse (like the user did with Night Light) — that's often more diagnostic than the original symptom description.

## 🎯 Completed Work — Client Questionnaire Reconciliation (2026-07-29)
The user shared `cuestionario-cliente-automotivo.docx` — a filled-out questionnaire from the actual shop client. It contained several concrete requirements that changed real parts of the app (not just additions). Read `PROJECT_CONTEXT.MD` for the authoritative version of each answer; this is the technical changelog.

- [x] **Kanban stages renamed to the shop's real 8-stage flow**: `Ingresado → En Revisión → En Cotización → Esperando Repuestos → En Reparación → Terminado → En Pruebas → Entregado` (was a guessed 5-stage flow). Touched: `EstadoKanban`/`ORDEN_ESTADOS` in `src/lib/types/index.ts`, `src/lib/kanban-colors.ts` (8 new `--color-stage-*` tokens in `globals.css`), seed data, dashboard stat logic (`!== "Entregado"` instead of the old `"Listo para Entrega"`), and the board grid (`sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-8`, was `lg:grid-cols-5`).
- [x] **Fixed plate-ownership-transfer bug**: registering a "new vehicle" whose plate already exists used to throw an error. Now `resolverClienteVehiculo()` in `src/lib/mock-db/index.ts` treats a matching plate as an ownership transfer — it re-points the *existing* `vehiculo` record (same `vehiculoId`) at the new client, so history (keyed by `vehiculoId`) survives the sale. Verified via API test: transferred BPP123 to a new client, confirmed 2 orders still show in its history under the new `clienteId`.
- [x] **Tri-state Cotización status + rejection follow-up**: `Cotizacion.aprobadaCliente: boolean` → `estado: "Pendiente" | "Aprobada" | "Rechazada"`, plus `seguimiento: boolean` and `fechaSeguimiento: string | null`. Rejecting shows a "Programar seguimiento" checkbox in `OrdenDetalleModal.tsx`; if checked, `setCotizacionEstado()` computes `fechaSeguimiento` as rejection date + 3 days. `getSeguimientosPendientes()` surfaces overdue follow-ups as a dashboard banner. A rejected quote can be reconsidered back to "Pendiente".
- [x] **Orden → Factura naming lifecycle + `pagada` field**: added `Cotizacion.pagada: boolean`. Document title is "Orden N°" by default; becomes "Factura N°" only once `estado === "Aprobada" && pagada`. `setCotizacionPagada()` guards against marking an unapproved quote paid. Reflected in `CotizacionPdfDocument.tsx` (title + Kilometraje/Combustible/Concepto/Entrada-Salida fields added to match the shop's real paper format) and `DescargarCotizacionPdfButton.tsx` (filename prefix + button label switch between "Orden"/"Factura").
- [x] **Reopening a delivered order**: new `OrdenTrabajo.tipoOrden: "Reparación" | "Garantía"` (every order has one, defaults to "Reparación") and `ordenOrigenId: string | null` for traceability. `reabrirOrden()` in mock-db + `POST /api/ordenes/[id]/reabrir` creates a brand-new order for the same vehicle (starts at "Ingresado") instead of rewinding the old card. "Reabrir Orden" button appears in `OrdenDetalleModal.tsx` only when `estadoKanban === "Entregado"`.
- [x] **New Orden intake fields**: `kilometraje`, `combustible` (`NivelCombustible`: Vacío/1/4/1/2/3/4/Lleno), `horaIngreso`, `horaSalida` — captured via `NuevaOrdenForm.tsx`, displayed in the detail modal and printed on the PDF.
- [x] **Mechanic-facing changes** (`OrdenCard.tsx` + `OrdenDetalleModal.tsx`): prices (`precioUnitario`, subtotals, totals) are now hidden from the Mechanic role everywhere — they still see the description/quantity/tipo of each line, just no colones. Added a **shared notes thread** (`OrdenTrabajo.notas: NotaOrden[]`, `agregarNotaOrden()`, `POST /api/ordenes/[id]/notas`) visible and addable by all 3 roles — this is how Owner/Secretary comments reach the mechanic and vice versa. Added a **vehicle history section** in the detail modal (`GET /api/vehiculos/[id]/historial`, new route) visible to all roles including Mechanic, showing prior visits (with prices hidden for Mechanic same as the current order).
- [x] Verified everything both via direct API calls and by driving the real UI in the browser as all 3 roles (confirmed price-hiding, the 8-column board, the historial/notes sections, the Orden→Factura title switch with a live-generated sample PDF, and the rejection→follow-up→reconsider cycle).
- [x] `npx tsc --noEmit` / `npm run lint` clean throughout.

## 🎯 Completed Work — Per-Card Activity Log (2026-07-30)
Client request: any worker should be able to see who did what on a shared Kanban card, since more than one person might work the same order (and the shop may hire more mechanics later, per the questionnaire).

- [x] **Named worker identity alongside role**: `src/lib/role-context.tsx` now stores a `nombre` (free-text name) next to `rol` in `localStorage`, set via `setIdentidad(rol, nombre)` (replaces the old `setRol`). `RoleSelector.tsx` asks for a name before picking a role (optional — falls back to the role label via the new `nombreMostrado(nombre, rol)` helper if left blank). `StaffNav.tsx` shows "Name · Role" and its logout button is now `cerrarSesion()` (was `setRol(null)`).
- [x] **Per-order audit trail**: new `OrdenTrabajo.actividad: ActividadOrden[]` (`{ autor, rol, accion, fecha, hora }`), separate from the existing manual `notas` thread — `actividad` is auto-logged by mock-db itself, not something a user writes. New `Actor` interface (`{ autor, rol }`) and `agregarActividad()` helper in `src/lib/mock-db/index.ts`; every mutating function that touches an order now takes an `actor: Actor` param and logs a descriptive entry: `crearOrden`, `reabrirOrden`, `moverOrdenEstado`, `agregarFotoOrden`, `crearCotizacion`, `setCotizacionEstado`, `setCotizacionPagada`.
- [x] **Threaded actor identity through every relevant API route** (`/api/ordenes`, `/api/ordenes/[id]`, `/api/ordenes/[id]/fotos`, `/api/ordenes/[id]/reabrir`, `/api/cotizaciones`, `/api/cotizaciones/[id]`) — all now require `{ autor, rol }` in the request body and return 400 if missing. The two Cotización endpoints now return `{ cotizacion, orden }` instead of just the quote, since quote actions log activity onto the *order*, not the quote itself, and the client needs the fresh `orden.actividad` back.
- [x] **New "Historial de Actividad" section** in `OrdenDetalleModal.tsx` — newest-first list of every action on the card, visible to all 3 roles. Also added a one-line "Última actividad: {autor}" indicator directly on `OrdenCard.tsx` so it's visible without opening the modal at all.
- [x] Verified via API test (3 different named actors — Ana/Secretary, Luis/Mechanic, Henry/Owner — performing create/move/quote actions, confirmed all 3 accumulate correctly on one order) and via the browser (entered a real name "Carlos Mecánico" at login, confirmed it shows in the nav, in the activity log, and updates the card's "Última actividad" line live after a status move).
- [x] `npx tsc --noEmit` / `npm run lint` clean.

## 🎯 Completed Work — Real Authentication (2026-07-30)
Client request: real per-employee login with proper access rights, plus a way to add future hires himself, and a "good layer of security by code" — not just UI hiding — for who can do what on a Kanban card. Chosen approach: **Auth.js (NextAuth) v5, self-hosted** (Option A from the cost-effectiveness comparison) — free forever, no third-party account needed, full control. See the approved plan at the time (`Plan: Self-Hosted Authentication with Auth.js (NextAuth) + Server-Enforced Roles`) for the full rationale/decisions if more context is ever needed.

- [x] **New dependencies**: `next-auth@beta` (v5, App Router-native `auth()`/`signIn()`/`signOut()`), `better-sqlite3` (single-file DB for user accounts), `bcryptjs` (pure-JS password hashing, no native build step). `next-auth@beta` (5.0.0-beta.32 at install time) officially supports Next 16 / React 19 per its peer deps — still tagged beta upstream but the standard way to use Auth.js with the App Router.
- [x] **New persistent user store** (`src/lib/db/users.ts`): a SQLite file at `data/taller.db` (gitignored), opened via a `globalThis` singleton (same pattern as the existing in-memory `mock-db`). One `users` table: `id, usuario (unique username), passwordHash, nombre, rol, debeCambiarPassword, passwordChangedAt, createdAt`. This is deliberately **separate and durable**, unlike the mock-db — credentials must survive a server restart even while the rest of the business data is still an in-memory mock.
  - **Login identifier is username, not email** — the Owner creates every account himself (no self-service sign-up), so a username is exactly as secure as an email for login purposes, and it means the app never needs an email-sending service (no SMTP, no cost). Tradeoff: no self-service "forgot password" email link — instead the **Owner resets a user's password from `/staff`**.
  - **Password policy**: minimum 8 characters, not purely numeric. **Passwords expire every 90 days** (`passwordExpirada()` checks `passwordChangedAt`) — same forced "change your password" screen as an Owner-triggered reset (`debeCambiarPassword`), no separate code path for expiry vs. reset.
  - First Owner account bootstrapped via a one-off script: `npx tsx scripts/seed-owner.ts <usuario> <password> <nombre>` (there's no public sign-up, so this is how the very first login gets created).
- [x] **Auth.js configuration split into two files** (`src/auth.config.ts` + `src/auth.ts`) — **required**, not optional: `src/middleware.ts` runs in the Edge runtime, which cannot load native modules (`better-sqlite3`) or `node:fs`. `auth.config.ts` is the Edge-safe subset (no providers, no DB) used only by middleware to verify/decode the JWT cookie; `auth.ts` spreads it and adds the Credentials provider + DB-backed callbacks, used everywhere else (Node runtime). **If you ever see a "Module not found: better-sqlite3" or "Edge runtime does not support Node.js 'fs' module" error from `src/middleware.ts`, this split is why — don't import `@/auth` (the full one) from middleware, only `@/auth.config`.**
  - Session strategy: JWT (no separate sessions table). The JWT/session embeds `usuario`, `rol`, and `debeCambiarPassword`.
- [x] **`src/middleware.ts`**: explicit allow-list of public paths (`/`, `/buscar/*`, `/api/auth/*`, and the exact path `/api/vehiculos` for the public plate-lookup — **not** `/api/vehiculos/[id]/historial`, which is staff-only). Everything else defaults to protected (redirects to `/` for pages, returns 401 JSON for `/api/*`) — a safer default than allow-everything-except-list.
- [x] **Replaced the fake login entirely**: `src/lib/role-context.tsx` keeps its exported shape (`{ rol, nombre, cerrarSesion, listo, debeCambiarPassword }`, `setIdentidad` removed) so none of its ~9 consumers needed changes — internally it now wraps `next-auth/react`'s `SessionProvider`/`useSession()` instead of `localStorage`. `RoleSelector.tsx` deleted; `src/app/page.tsx` now renders a real username+password sign-in form (`SignInForm.tsx` via `IngresoPanel.tsx`, which also auto-redirects an already-signed-in visitor straight to `/dashboard`).
- [x] **Forced password-change screen** (`src/app/(dashboard)/cambiar-password/page.tsx` + `src/app/api/cuenta/password/route.ts`): triggers automatically (via a redirect in `(dashboard)/layout.tsx`) whenever `debeCambiarPassword` is true. Requires the current password (not just an active session) to set a new one. **Important lesson learned**: don't try to "hot-patch" the existing JWT cookie in place via `useSession().update()` after a password change and then client-navigate — there's a real race where the dashboard layout's guard re-reads stale in-memory session state and bounces back to the change-password screen before the update propagates (reproduced this exact loop while testing). The robust fix that's now in place: after a successful change, just `signOut()` and require a fresh login with the new password — simpler and race-free.
- [x] **`/staff` admin page + `/api/staff` route** (Owner-only, both client- and server-checked): lets the Owner create new staff accounts in-app (username, temp password, nombre, rol) and reset an existing user's password — no external dashboard needed, satisfies "add more users for future hires" directly.
- [x] **Server-side identity in every mutating API route** — the actual "security by code" ask. New `src/lib/auth/getActorFromSession.ts` (`getActorFromSession()` / `requireActor()`) derives `{ autor, rol }` from the verified Auth.js session instead of trusting a client-supplied request body. Migrated all 7 order/quote routes (`ordenes`, `ordenes/[id]` — the Kanban move endpoint, `ordenes/[id]/notas`, `ordenes/[id]/fotos`, `ordenes/[id]/reabrir`, `cotizaciones`, `cotizaciones/[id]`). The frontend no longer sends `autor`/`rol` in any request body — `KanbanBoard.tsx`, `NuevaOrdenForm.tsx`, and `OrdenDetalleModal.tsx` had their `actor`/`nombreMostrado` plumbing removed accordingly.
- [x] **Server-side price redaction for Mechanic role** (`src/lib/cotizacion-redaccion.ts`'s `ocultarCotizacionSiMecanico()`) — closes the gap where a Mechanic-role session could previously read quote totals straight out of a raw API response or the dashboard's initial server-rendered payload, even though the UI already hid them. Applied to: the dashboard page's initial `OrdenConDetalle[]` props (the biggest leak — Server Components ship full data in the initial payload regardless of client-side hiding), `GET /api/cotizaciones` (returns `[]` for Mechanic), and `GET /api/vehiculos/[id]/historial` (strips `cotizacion` per history entry). Also hid the "Cotiz. por Decidir" and "Facturado" dashboard stat tiles from Mechanic.
- [x] **Verified end-to-end** (via curl with cookie jars, since the browser automation tool was unreliable this session — see Blockers): all 3 roles sign in/out correctly; Kanban card moves attribute to the real signed-in name (confirmed unspoofable — tried moving a card with no session, got 401); Mechanic's dashboard HTML and `/api/vehiculos/[id]/historial` contain **zero** price figures while Owner's contain them; `/api/cotizaciones` returns `[]` for Mechanic; non-Owner gets 403 from `/api/staff`; unauthenticated requests to `/dashboard`/`/staff` redirect, `/buscar` and `GET /api/vehiculos?placa=...` stay public, unauthenticated `POST /api/ordenes` returns 401; forced password-change triggers correctly both from an Owner-set temp password and from a backdated (91-day-old) `passwordChangedAt`; user accounts survive a full dev-server restart (proves SQLite persistence, unlike the in-memory mock-db).
- [x] `npx tsc --noEmit` / `npm run lint` clean throughout.

## 🎯 Completed Work — Full Mobile Responsive Pass (2026-07-30)
Client requirement: the app must feel smooth on mobile browsers, not just PC — explicitly asked for a full pass across every screen, hamburger menus where needed, no compromise on the desktop experience.

- [x] **`StaffNav.tsx` — real hamburger menu.** Below `sm` (640px) the inline nav (Panel/Agendar Cita/Personal + name·role·"Cambiar") collapses into a hamburger toggle button; tapping it opens a stacked mobile panel with the same links plus the logout control. Desktop nav (`sm:flex`) is untouched — confirmed via a direct DOM check that the 8-column Kanban grid and 5-tile stat row still render at full column count at 1280px width, nothing regressed.
- [x] **Fixed every hardcoded `grid-cols-N` form layout** that would've squeezed inputs uncomfortably narrow on a phone — these previously had no responsive prefix at all (applied at every viewport):
  - `NuevaOrdenForm.tsx`: client-data grid, vehicle-data grid, and the tipoOrden/kilometraje/combustible row → `grid-cols-1 sm:grid-cols-2` / `sm:grid-cols-3`.
  - `CitaScheduler.tsx`: same treatment for its client-data, vehicle-data, and fecha/hora/motivo groups.
  - `OrdenDetalleModal.tsx`'s cotización line-item row was the worst offender — a **fixed pixel grid** (`grid-cols-[1fr_7rem_4rem_6rem_1.5rem]`) that would overflow horizontally on any phone. Rebuilt as `grid-cols-2` on mobile (descripción spans both columns on its own row, tipo/cantidad on the next row, precio/quitar-button on the row after) reverting to the original tight single-row grid at `sm:` and up.
- [x] **Stacked flex rows that would've crammed on narrow screens**: the "reabrir orden" tipo-select + confirm button (`OrdenDetalleModal.tsx`), the Owner's password-reset input+buttons (`staff/page.tsx`, given `flex-wrap` + `min-w-0 flex-1` on the input so it can actually shrink), and several `justify-between` rows pairing a long label with a right-aligned value (activity log entries, vehicle-history entries, quote decision status+buttons, buscar's plate/status badges) — all switched to `flex-wrap` with a gap so they wrap onto a second line instead of squeezing or overflowing.
- [x] **Tightened outer page/modal padding on mobile** (`px-6 py-8/10/16` → `px-4 py-6 sm:px-6 sm:py-8` style, same pattern everywhere) across the dashboard, citas, staff, and buscar pages, and both modals (`NuevaOrdenForm`, `OrdenDetalleModal`) — more usable width on a 375px phone without changing anything at `sm:` and up.
- [x] **`(portal)/layout.tsx`**: hid the "Portal de Clientes" header label below `sm:` (same pattern StaffNav already used for its own secondary label), tightened header padding.
- [x] **Verified in-browser at a 375px viewport** (no horizontal page overflow on any screen — landing, dashboard, citas, staff, buscar, and with the Nueva Orden / Orden Detalle modals open) and re-confirmed the **desktop** layout is unchanged at 1280px (8-column Kanban, 5-tile stat row still render at full column count).
- [x] `npx tsc --noEmit` / `npm run lint` clean throughout.

## 🎯 Completed Work — GitHub Setup, Real Logo, Cita Status, Final Loose Ends (2026-07-31)

**Git/GitHub:**
- [x] Initialized a local git repo (there was none before this — every prior session's work lived only on disk with no history/backup). `.gitignore` already excluded `.env*`, `/data` (the SQLite users DB), and now also `.claude/settings.local.json` (machine-specific tool permissions, not shared config).
- [x] Pushed to `https://github.com/TUNGARX/app_taller` on `main`. **Recommendation given to the user**: connect DigitalOcean App Platform directly to this GitHub repo for auto-deploy-on-push — simpler and safer than manual server management, with easy rollback to any prior commit.
- [x] Switched from HTTPS to **SSH** for git auth per the user's request — generated a dedicated `ed25519` key (`~/.ssh/id_ed25519_github`), user added the public key to their GitHub account, verified with `ssh -T git@github.com`. See Blockers for a real gotcha hit while setting up HTTPS auth first.

**Real logo:**
- [x] Client sent their actual logo (car outline + "automotivo" wordmark + "Nuestro motivo sos vos" slogan) — saved to `public/logo.jpg`. **This also confirms `TALLER_CONFIG`'s business info was correct** (name and slogan match the real logo exactly) — no changes needed there.
- [x] Replaced the placeholder "TALLER." text wordmark with the real logo image (wrapped in a small white rounded badge for contrast, since the logo has a white background and the nav/landing/portal all use the dark `steel` theme) in: `StaffNav.tsx`, `(portal)/layout.tsx`, and the landing page hero (`app/page.tsx`). Also added it to the PDF header (`CotizacionPdfDocument.tsx`, via `@react-pdf/renderer`'s `Image`) and updated the browser tab title/metadata to "Automotivo - Gestión de Taller".
- [x] **Found and fixed a real middleware bug while testing this**: `src/middleware.ts`'s matcher only excluded `_next/static`/`_next/image`/`favicon.ico` — any *other* static file in `/public` (like the new logo) was being intercepted by the auth middleware and redirected to `/` for unauthenticated requests, since it wasn't in the explicit public-paths allow-list. Fixed by broadening the matcher to exclude any path with a file extension (`.*\..*$`), the standard Next.js middleware pattern — this would have silently broken any future image/asset added to `/public` for a logged-out visitor (the landing page and portal logo both needed this).
- [x] Dirup data migration: **client will handle this separately and start fresh with this app** — dropped from next steps, nothing to build here.
- [x] Cotización line-item editing: **client confirmed the current write-once-after-creation behavior is fine** — dropped from next steps.

**Cita status progression:**
- [x] New `setCitaEstado()` in `src/lib/mock-db/index.ts` + `PATCH /api/citas/[id]` route — lets staff advance a Cita through `Programada → Confirmada → Completada`, or cancel it at any point. Restricted server-side to Owner/Secretary (a Mechanic gets a 403, matching the existing `RoleGuard` on the `/citas` page) via `requireActor()` + an explicit role check.
- [x] `CitaScheduler.tsx`: each upcoming appointment now shows status-appropriate action buttons (Programada → Confirmar/Cancelar; Confirmada → Marcar Completada/Cancelar; terminal states show none), calling the new route and updating local state from the response.
- [x] Verified end-to-end in the browser (advanced a real appointment from Programada → Confirmada, confirmed the badge and buttons updated correctly) and via curl (confirmed a Mechanic session gets 403).
- [x] `npx tsc --noEmit` / `npm run lint` clean throughout.

**Public portal photos + Factura download:**
- [x] `(portal)/buscar/page.tsx` now renders each order's `fotos` as thumbnails (both the current/most recent order and every entry in "Historial de Servicio") — no backend change needed, `GET /api/vehiculos?placa=` already returned the full `orden` object including `fotos` via `getHistorialVehiculo()`, it just wasn't being rendered on this page yet.
- [x] Reused the existing `DescargarCotizacionPdfButton` component (previously only wired into the staff-only `OrdenDetalleModal`) on the public portal too — shown whenever an order's cotización is `estado === "Aprobada" && pagada` (i.e. it's actually a Factura), for both the current order and any past paid order in the history list. No new component needed, no auth required (`/buscar` and its API are and remain fully public — reverified with zero cookies after this change).
- [x] Verified live in the browser: searched a real seeded plate (BPP123) with both photos and a paid Factura, confirmed the thumbnails render, and confirmed the PDF button generates without error (checked console for errors — none).
- [x] `npx tsc --noEmit` / `npm run lint` clean.

## 🎯 Completed Work — Light/Dark Mode (2026-07-31)
Client request: an aesthetic light/dark toggle, working on both PC and mobile. This app already used semantic CSS-variable tokens (`bg-bg`, `text-ink`, `bg-paper`) throughout via Tailwind's `@theme inline`, which made this far less invasive than a typical dark-mode retrofit — most surfaces flip automatically once the tokens themselves are redefined.

- [x] **`src/app/globals.css`**: added a `.dark { ... }` block redefining `--color-bg` (#17161a), `--color-ink` (#eae7e2), `--color-paper` (#232227) — the three "light chrome" surface tokens. **Brand/accent colors (`steel`, `steel-light`, `safety`, `amber`) and the 8 Kanban stage colors are unchanged between themes** — they're already dark/vivid enough to read correctly on either background, and the nav/hero/hero-buttons that use `bg-steel` + `text-white` were already "dark mode" by design, so they needed zero changes. Added `@custom-variant dark (&:where(.dark, .dark *));` (Tailwind v4's manual-toggle recipe) for any future spot-fix that can't be tokenized. `color-scheme` on `:root`/`.dark` and on form controls now follows the active theme (`inherit`) instead of being hardcoded to `light`.
- [x] **No-flash-of-wrong-theme on load**: `src/lib/theme-script.ts` exports a tiny inline script (reads `localStorage["taller-theme"]`, falls back to `prefers-color-scheme`, adds `.dark` to `<html>` before paint) injected via a `<script>` tag in a manual `<head>` in `src/app/layout.tsx` (with `suppressHydrationWarning` on `<html>` since the script mutates its class before React hydrates).
- [x] **`src/components/ThemeToggle.tsx`**: sun/moon icon button, toggles the `.dark` class + persists to `localStorage`, styled to match the existing header icon-button pattern (all 3 current placements sit on the dark `steel` header/hero). Wired into `StaffNav.tsx` (next to the hamburger toggle, visible at every width), `(portal)/layout.tsx`'s header, and the landing page hero (`app/page.tsx`, absolute-positioned top-right).
- [x] **Systematic color audit across 13 files** (58 occurrences of hardcoded `border-black/*` / `bg-white` / `bg-black`) — converted the ones tied to light-mode assumptions: `border-black/NN` → `border-ink/NN` (borders/hairlines, so they stay visible against either background), and standalone `bg-white` (card/input surfaces in `staff/page.tsx`, `cambiar-password/page.tsx`) → `bg-paper`. **Deliberately left unchanged**: the 3 logo badges (`bg-white`, since the logo JPEG has a literal white background baked in and must stay white regardless of theme), the 2 modal backdrop overlays (`bg-black/40`, a dimming scrim that reads fine in both themes), and everything on the always-dark `steel` nav/hero/landing surfaces (their `text-white`/`border-white/15`/`hover:bg-white/10` were already correct for a permanently-dark surface).
- [x] **Found and fixed a real bug during the audit**: the "plate badge" pattern (`bg-ink` + `text-white`, meant to look like a physical dark license plate) in `buscar/page.tsx`, `OrdenDetalleModal.tsx`, and `OrdenCard.tsx` would have silently broken in dark mode — `--color-ink` flips to a near-white value there, which would've turned the badge into a *light* background with invisible white-on-white text. Fixed by switching those three specifically to `bg-steel`/`border-steel-light` (the always-dark chrome tokens) instead of `bg-ink`.
- [x] **Verified end-to-end** at both desktop (1280px) and mobile (375px) viewports, in both themes, on every screen: landing, portal/`buscar` (confirmed dark mode persists on this fully public, unauthenticated page too), dashboard/Kanban (cards, plate badges), both modals (Nueva Orden, Orden Detalle), citas, staff. Confirmed the toggle button itself works via a real click (not just `localStorage` manipulation) in both directions, and confirmed zero horizontal overflow introduced at mobile width in either theme.
- [x] `npx tsc --noEmit` / `npm run lint` clean throughout.

## 🎯 Completed Work — Delete Staff Account + Kanban "Entregado" Clarification (2026-07-31)

**Delete-account button:**
- [x] `src/lib/db/users.ts`: new `eliminarUsuario(id)` — hard-deletes the row from the `users` SQLite table. **Guarded against deleting the last remaining Owner** (`contarOwners()` check — refuses if the target is an Owner and no other Owner exists), so the shop can never be left with zero admin accounts. Past `ActividadOrden`/`NotaOrden` entries are untouched by design — they store the person's name as plain text at the time of the action, not a foreign key to this table, so deleting an account never rewrites or breaks historical "who did what" records.
- [x] `DELETE /api/staff` (same collection route as GET/POST/PATCH, body `{ id }`) — Owner-only (`requerirOwner()`), and additionally **refuses to let the signed-in Owner delete their own account** (compares the target's `usuario` to the session's) so nobody can accidentally lock themselves out mid-session.
- [x] `staff/page.tsx`: "Eliminar cuenta" button next to "Reiniciar contraseña" on each row in Cuentas Existentes, with an inline confirm step ("¿Eliminar a {nombre}?" + Sí eliminar/Cancelar) matching the existing reset-password interaction pattern rather than a native `confirm()` dialog.
- [x] Verified via curl (self-delete → 400, last-Owner delete → blocked at the `eliminarUsuario` level directly, confirmed via a standalone script) and in the browser (created/deleted a real Mechanic test account, confirmed the confirm-step UI, confirmed dark mode renders it correctly too).
- [x] `npx tsc --noEmit` / `npm run lint` clean.

**Kanban "Entregado" behavior — clarified, then implemented:**
- [x] Confirmed by reading the code (not guessed) that there was previously no auto-archive/hide at all — flagged it as an open product decision, then the user decided: **hide Entregado cards 72 hours after delivery, with a way to reveal and return them for warranty claims.**
- [x] `src/lib/format.ts`: new `horasDesde(fechaIso, hora)` — hours elapsed since a date+time, used generically (not Entregado-specific) in case another archiving rule needs it later.
- [x] `KanbanBoard.tsx`: `estaArchivada(detalle)` — true only for `Entregado` orders where `horasDesde(fechaEntrega, horaSalida) > 72`. The Entregado column filters these out by default; a dashed-border toggle button appears above the column ("+N entregadas hace +72h — Mostrar" / "Ocultar entregas antiguas") to reveal them. **Nothing is deleted or hidden from the data layer** — this is purely a board-rendering filter, the orders still exist and are still returned by every API route exactly as before.
- [x] `OrdenCard.tsx`: archived cards get a small "+72h" badge, and — since stepping back through all 7 intermediate Kanban stages one click at a time would be impractical for a warranty return — a dedicated amber "↩ Cliente regresó — Volver a Ingresado" button that jumps the order **directly** back to `Ingresado` in one action (reuses the same `PATCH /api/ordenes/[id]` endpoint the normal Anterior/Siguiente buttons use, just with a non-adjacent target state, which the API already allowed).
- [x] Refactored `KanbanBoard.tsx`'s move logic: `mover(id, direccion)` (the existing ±1 stepping) and the new direct jump both now go through one shared `actualizarEstado(id, estado)` function instead of duplicating the fetch/error-handling logic.
- [x] Verified live in the browser: both seeded `Entregado` test orders (4–6 days old) were correctly hidden by default; the reveal toggle showed them with the "+72h" badge; clicking "Volver a Ingresado" on one moved it straight back to the Ingresado column (confirmed via the API too — `estadoKanban` changed with `fechaEntrega`/`horaSalida` left untouched as history). Also confirmed correct rendering in dark mode and at 375px mobile width, and that the price/activity-log data on the returned card survived the move intact.
- [x] `npx tsc --noEmit` / `npm run lint` clean.

## 🎯 Completed Work — Kanban Plate Search Bar (2026-07-31)
Client request: a find bar next to "+ Nueva Orden" on the Panel to locate a card by plate, highlighted — flagged by the user as especially useful once the list of 72h-hidden Entregado cards grows large.

- [x] **`KanbanBoard.tsx`**: new `busqueda` state + a text input (placeholder "Buscar por placa...") in the header row next to "+ Nueva Orden", with a "×" clear button that appears once there's a query. `coincide(detalle)` does a case-insensitive substring match against `vehiculo.placa`.
- [x] **Matching/dimming**: every `OrdenCard` gets `resaltada` (query matches this plate) and `atenuada` (a query is active and this plate does *not* match) booleans computed from `coincide()`. `OrdenCard.tsx` renders `resaltada` as a `ring-2 ring-safety` outline and `atenuada` as `opacity-30`, so a match visually pops out of a full column instead of the list being hard-filtered down.
- [x] **Archived-card auto-reveal**: while a search is active, the Entregado column's archive filter (`estaArchivada()`, see the 72h auto-archiving section above) is bypassed for any card that matches the query — `columnaCompleta.filter((d) => !estaArchivada(d) || (hayBusqueda && coincide(d)))`. This is the main point of the feature per the user's own framing: no need to click "Mostrar" and scroll a big pile of old deliveries just to find one plate that came back for warranty work.
- [x] Search is available to all three roles (not gated) — it's read-only, doesn't touch server state, same as the existing Anterior/Siguiente card navigation.
- [x] **Verified live in the browser** at both mobile (375px, input wraps cleanly next to the button, confirmed via `read_page`) and a wider desktop-ish width (confirmed via bounding-rect check, no overflow): searching "CTG" rings `CTG890` and dims the other 4 visible cards; searching "CL456" reveals the archived `CL456789` (confirmed hidden by default beforehand by toggling "Mostrar"/"Ocultar" directly) with the ring applied despite it never having been manually revealed.
  - Note: this session's `javascript_tool`, when reading `getComputedStyle(...).opacity` on the cards, reported `"0"` for every card regardless of match state — traced to the tab being backgrounded, which stalls the `rise-in` CSS keyframe animation's fill state (`animationName` stayed `"rise-in"` indefinitely). This was **not** a real bug — checking the actual `className` string directly (`opacity-30`/`opacity-100`/`ring-2 ring-safety`) confirmed the logic was applying correctly; the earlier "Blockers" note about this browser tool's visual-verification quirks (screenshots, stale refs) applies here too — prefer reading `className`/DOM state directly over trusting `getComputedStyle` opacity/animation values when the tab may not be focused.
- [x] `npx tsc --noEmit` / `npm run lint` clean. Committed as `21f9ac2`, pushed to `origin/main`.

## 🎯 Completed Work — Hide Old Completada Citas (2026-07-31)
Client request: in "Agendar Cita"'s "Próximas Citas" list, completed appointments must drop off after 1 month so the list doesn't grow forever.

- [x] **`CitaScheduler.tsx`**: reused the existing `horasDesde(fechaIso, hora)` helper from `src/lib/format.ts` (same one the 72h Entregado auto-archiving already uses) — `estaVencida(cita)` is true when `cita.estado === "Completada"` and `horasDesde(cita.fecha, cita.hora) > 24 * 30` (~1 month). The "Próximas Citas" list filters these out before rendering (`citas.filter(({ cita }) => !estaVencida(cita))`).
- [x] **Purely a display filter, same as Entregado** — nothing is deleted from `citas` state or the mock-db; a `Programada`/`Confirmada`/`Cancelada` cita is never affected regardless of age, only a `Completada` one older than a month. Unlike the Kanban Entregado feature, the user did not ask for a reveal/unhide toggle here, so none was added — keep it that way unless asked.
- [x] Verified in the browser: current seeded Completada citas (all within the last ~9 days) still show correctly, confirming the filter doesn't over-trigger; the underlying date math is the same helper already verified working for the 72h Kanban case.
- [x] `npx tsc --noEmit` / `npm run lint` clean.

## 📋 Next Steps (Immediate Priorities for Next Session)
1. **🎯 Google Drive/Sheets integration (user's stated plan):** architecture decided — a **Google Cloud service account** (see Status section above), shared access to Sheets/Drive resources directly, no OAuth consent flow. Set up the Cloud project + service account under the developer's own Google account for now; create the 5 Sheets tabs (`Clientes`, `Vehiculos`, `Citas`, `Ordenes_Trabajo`, `Cotizaciones`) plus a Drive folder for evidence photos, matching `src/lib/types/index.ts` (now includes the questionnaire's new fields — make sure the real Sheets columns match, e.g. `tipoOrden`, `kilometraje`, `combustible`, `horaIngreso`/`horaSalida`, `notas`, `actividad`, `estado`/`pagada`/`seguimiento` on Cotizaciones). `actividad` and `notas` are both arrays-of-objects on a single order — decide whether that's a JSON-blob cell, a nested sheet, or a separate "Actividad" tab keyed by `ordenId` before modeling it in Sheets. Build the real Sheets/Drive API integration to replace `src/lib/mock-db` — the `index.ts` accessor/mutation functions are the exact swap boundary; nothing above that layer should need to change if done right. **When ready to move to the client's real account: create the same Sheets/folder in their Drive, share with the service account's email, swap the ID env vars — no code changes.**
2. **Google Calendar integration for citas** — per the questionnaire, appointment reminders should sync to Google Calendar (not WhatsApp/email). Same service-account approach: share the shop's calendar with the service account (works for a regular Gmail account too, no Workspace/domain-wide-delegation needed since it's calendar-sharing, not org-wide access).
3. **Then: deploy to DigitalOcean App Platform** (connect it to the now-existing GitHub repo for auto-deploy), per the business plan in `PROJECT_CONTEXT.MD`.
4. **Auth fast-follow (not urgent, note for whenever it comes up):** the Owner currently manages staff exclusively through the in-app `/staff` page — no external dashboard to fall back on, which is by design for this option. If email-based password reset is ever wanted, that requires adding an SMTP integration and switching the login identifier from username to email — a real design change, not a small tweak, so don't do it casually.
5. **`data/taller.db` needs to be part of the DigitalOcean deploy/backup plan** — unlike the in-memory mock-db (expected to reset), this SQLite file holds real user credentials and must persist across deploys and be included in whatever backup strategy the shop ends up with.

## ⚠️ Blockers & Technical Notes
- **All Cotización money math must go through `src/lib/cotizacion-calc.ts`'s `calcularTotalesCotizacion()`.** Don't recompute subtotal/IVA/total inline anywhere else (UI or PDF) — that's exactly how the on-screen total and the PDF total could silently drift apart.
- **Cotizacion no longer has `aprobadaCliente`** — it's `estado: EstadoCotizacion` ("Pendiente"/"Aprobada"/"Rechazada") plus separate `pagada`/`seguimiento`/`fechaSeguimiento` fields. If you see `aprobadaCliente` referenced anywhere, it's stale code.
- **"Factura" vs "Orden" is derived, never stored** — it's always `cotizacion.estado === "Aprobada" && cotizacion.pagada`, computed wherever it's needed (modal, PDF document, download filename). Don't add a separate `esFactura` field to the data model; that would create a second source of truth that could drift from the actual estado/pagada values.
- **Kanban stage names changed** — if you find `"Por Revisar"`, `"En Diagnóstico"`, or `"Listo para Entrega"` anywhere, it's stale code from before the questionnaire rework; the real 8 stages are in `ORDEN_ESTADOS`.
- **Mutable mock data must go through `src/lib/mock-db/index.ts`'s `globalThis` singleton.** Do not import the individual files in `src/lib/mock-db/clientes.ts` etc. directly if you need writes to be visible elsewhere — always go through the `index.ts` exports.
- **Role gating and identity are now server-enforced, not client-only.** (Superseded note, kept for history: this used to be a client-only `localStorage`/UI-hiding simulation with zero server checks — that's fully replaced now. `role-context.tsx` reads real Auth.js sessions, `src/middleware.ts` protects routes at the edge, and every mutating API route derives `{ autor, rol }` from `getActorFromSession()`/`requireActor()` — a request body can no longer forge who performed an action.)
- **`src/middleware.ts` must only import `@/auth.config` (the edge-safe split), never `@/auth` directly** — the full config pulls in `better-sqlite3`/`node:fs` via `src/lib/db/users.ts`, which crashes the Edge runtime. See the "Real Authentication" section above if this trips anyone up again.
- **The Claude_Browser automation tool was unreliable this session** (stale element refs across navigations, screenshots timing out, tabs silently disappearing) — the auth flows ended up being verified via `curl` with cookie jars instead, which worked reliably. If browser automation acts up again, don't fight it — a scripted curl session against the dev server is a solid fallback for anything involving cookies/sessions.
- **Same tool also showed an unexplained viewport/breakpoint quirk during the mobile pass**: at a resized 375px viewport, elements using `sm:grid-cols-2` sometimes still rendered as 2 columns (confirmed via actual element bounding-box positions, not just computed style) even though `sm:grid-cols-3` on an identical-pattern element in the same page correctly stayed single-column, and `window.matchMedia('(min-width: 640px)').matches` correctly reported `false`. That inconsistency (same breakpoint, same viewport, different behavior for `-2` vs `-3`) points to a sandbox/DPI-scaling artifact in this specific tool rather than a real CSS bug — the source is confirmed correct and matches the identical responsive pattern already used elsewhere in this codebase (the Kanban board grid, the staff form). No page ever showed actual horizontal overflow in any test. **Still, do a quick real-phone or real-Chrome-DevTools-device-emulation check of the multi-field forms (Nueva Orden, Agendar Cita, crear cotización) before fully trusting this pass** — that's the one thing this session's tooling couldn't give a fully trustworthy visual confirmation of.
- **Git Credential Manager gotcha hit while setting up GitHub auth**: an early attempt to fix a failed push set `git config --global credential.https://github.com.provider generic`, which forces GCM's legacy basic-auth prompt (username+password) instead of letting it auto-detect github.com and use the proper OAuth/browser flow — GitHub rejects basic auth outright ("Password authentication is not supported"), so every push failed instantly with no visible prompt at all. Fixed by removing that override (`git config --global --unset credential.https://github.com.provider`); after that, `git push` correctly triggered GCM's browser-based GitHub login on the first real attempt. **If a fresh push to GitHub fails instantly with "Invalid username or token" and no browser window appears, check for this override first** before assuming credentials are missing or corrupted.
- The project folder name ("APP TALLER") contains a space and capital letters, which `create-next-app` rejects as an npm package name. If re-scaffolding anything, scaffold in a temp dir and move files in.
- All in-memory writes reset on dev server restart — this is expected and fine for prototyping.
- Photo evidence is stored as base64 `data:` URLs in memory for now; real implementation will use Google Drive public preview links (`https://googleusercontent.com`) once that integration is built. Don't be surprised if the mock DB's memory footprint grows if many photos are uploaded during a long dev session — it's not persisted anywhere, just RAM.
- Ensure local environment variables (`.env`) are securely mapped out to facilitate a seamless production migration to DigitalOcean App Platform down the road.
